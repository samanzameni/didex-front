import { ConvertToTimezonePipe } from './convert-to-timezone.pipe';

describe('ConvertToTimezonePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToTimezonePipe();
    expect(pipe).toBeTruthy();
  });
});
