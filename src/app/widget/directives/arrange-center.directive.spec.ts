import { ArrangeCenterDirective } from './arrange-center.directive';

describe('ArrangeCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrangeCenterDirective();
    expect(directive).toBeTruthy();
  });
});
