import { ArrangeInlineWrapDirective } from './arrange-inline-wrap.directive';

describe('ArrangeInlineWrapDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrangeInlineWrapDirective();
    expect(directive).toBeTruthy();
  });
});
