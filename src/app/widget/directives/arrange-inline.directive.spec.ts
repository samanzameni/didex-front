import { ArrangeInlineDirective } from './arrange-inline.directive';

describe('ArrangeInlineDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrangeInlineDirective();
    expect(directive).toBeTruthy();
  });
});
