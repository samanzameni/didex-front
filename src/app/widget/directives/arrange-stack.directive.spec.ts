import { ArrangeStackDirective } from './arrange-stack.directive';

describe('ArrangeStackDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrangeStackDirective();
    expect(directive).toBeTruthy();
  });
});
